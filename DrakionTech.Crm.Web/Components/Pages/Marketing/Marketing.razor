@page "/marketing"
@attribute [Authorize]

@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("/marketing/publicaciones", replace: true);
    }
}